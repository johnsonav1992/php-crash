<?php

$numbers = [1, 2, 3, 4];
$fruit = array('orange', 'apple');

//associative arrays

echo $fruit[0];
$hex = [
    'red' => '#f00',
    'blue' => '#0f0'
];

var_dump($hex);

//json parsing
var_dump(json_encode($hex));
